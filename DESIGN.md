# DESIGN.md — MOA 비주얼 시스템

> **정본은 디자이너 원본 `프로토타입_0804.html`이다.** 이 문서는 그 파일에서 값을 읽어 규칙으로 옮긴 것이지, 새로 제안하는 문서가 아니다. 값이 어긋나면 원본이 이긴다.
>
> 갱신 2026-08-05 · 기획 근거: [00_서비스개요](reference/기획/00_서비스개요.md) · [01_고객/03_타깃고객](reference/기획/01_고객/03_타깃고객.md) · [02_시장/01_시장경쟁분석](reference/기획/02_시장/01_시장경쟁분석.md)

⚠ **원본이 저장소 밖에 있다.** `프로토타입_0804.html`은 이 저장소에 없다. 정본이 개인 파일로만 존재하면 누구도 대조할 수 없다. `reference/design/` 아래에 커밋하는 것이 §7의 첫 번째 할 일이다.

---

## 0. 브리프 (채워진 프롬프트)

> 내 브랜드는 **"이번 달 지킬 돈을 스스로 정하고, 정한 한도를 넘긴 만큼만 조용히 차감하며 월말까지 그 돈을 지켜내도록 돕는 소비 관리 서비스 MOA"** 이고,
> 주 고객은 **"스트레스로 배달·쇼핑·택시를 쓴 뒤 후회하지만 무엇을 줄여야 할지 몰라, 가계부·무지출 챌린지를 시도했다가 정착하지 못한 20~30대 임금근로 직장인(검증 모집 25~39세)"** 입니다.

---

## 1. 색 — 디자이너 원본 팔레트

### 주조색·배경·본문

| 역할 | 값 | 원본에서의 쓰임 |
|---|---|---|
| **주조색** | `#00B14F` (변수명 `--blue`, 값은 초록) | 버튼 면, 게이지 채움, 선택 상태 테두리, 탭 활성, 로고 화면 배경 |
| **배경** | `#F8F9FA` | 앱 전체 바닥. 카드(`#FFFFFF`)가 이 위에 뜬다 |
| **본문 글자** | `#0E0F0C` | 제목·금액·리스트 본문 (배경 위 18.2:1) |

보조 텍스트는 `--t2 #454745`(설명문), `--t3 #868685`(캡션·라벨). 면 색은 `--blue-weak #E2F6D5`(연한 초록)와 `--blue-dark #163300`(그 위의 글자) 한 쌍으로 움직인다. 의미색은 `--red #D03238`, `--amber #E68A2E`.

### ⚠ 원본 주석과 값이 어긋난다

원본 `:root` 첫 줄 주석은 이렇게 적혀 있다.

```text
/* A안 제약조건: 브랜드=세이지 그린, 배경=오프크림 (변수명은 호환을 위해 유지) */
```

그런데 실제 값은 **세이지 그린도 오프크림도 아니다.**

| 주석이 말하는 것 | 실제 값 | 차이 |
|---|---|---|
| 세이지 그린 (탁하고 어두운 회녹색) | `#00B14F` | 채도가 최대에 가까운 선명한 초록 |
| 오프크림 (따뜻한 미색) | `#F8F9FA` | 푸른기가 도는 차가운 회색 |

**둘 중 하나는 갱신이 안 된 것이다.** 주석이 의도이고 값이 미반영이면 팔레트 전체가 바뀌고, 값이 확정이고 주석이 잔재라면 주석을 지워야 한다. **디자이너 확인이 필요하고, 그 전까지는 값(`#00B14F`/`#F8F9FA`)을 기준으로 구현한다.**

> 참고 — 이 문서의 이전 판은 주조색으로 딥 플럼 `#5B3A6E`, 배경으로 웜 아이보리 `#F7F5F2`를 제안했다. **디자이너 원본이 기준이 된 지금 그 제안은 폐기한다.** 다만 "오프크림 배경"이라는 원본 주석의 방향은 그 제안과 같은 지점을 가리키고 있어, 위 확인 결과에 따라 다시 논의될 수 있다.

### 지켜야 할 대비 규칙

`#00B14F`는 **면으로 쓸 때와 글자로 쓸 때 값이 달라야 한다.** 원본은 `.btn-primary{background:var(--blue);color:#fff}`인데 이 조합은 **2.84:1로 KWCAG 4.5:1에 미달한다.** 코드는 이미 이 문제를 우회해 두었다 — §6 참조.

| 조합 | 대비 | 판정 |
|---|---:|---|
| `#0E0F0C` 본문 on `#F8F9FA` | 18.2:1 | 통과 |
| 흰 글자 on `#00B14F` | 2.84:1 | **미달** → 면은 `#00893E`(4.52:1) 사용 |
| `#868685` on `#F8F9FA` | 4.5:1 미달 | 글자로 쓸 땐 `#6B6E6B` 사용 |

아이콘·그래프 같은 **비텍스트는 이 검사 대상이 아니므로** 원본의 `#00B14F`를 그대로 쓴다. 글자로 칠할 때만 어두운 값으로 바꾼다.

---

## 2. 글꼴 — 두 벌 체계

| 역할 | 글꼴 | 원본에서의 쓰임 |
|---|---|---|
| **로고·워드마크** | **Plus Jakarta Sans** 800 | 스플래시의 `MOA` 한 곳 (60px, letter-spacing −1.2px) |
| **본문·UI 전체** | **Pretendard** (Variable) | 그 외 전부 |

디자이너는 요청 조건(Inter·Roboto·시스템 기본 제외)을 **로고에서만** 벗어나는 방식으로 풀었다. 브랜드 개성은 워드마크가 지고, 나머지는 읽기에 안전한 Pretendard가 받는다.

### 타이포 규칙 (원본에서 그대로 가져옴)

```css
body { word-break: keep-all; letter-spacing: -0.04em; }
/* 숫자·영문 표시 요소는 자간 0 */
.hero .big, .gh-num, .stat .v, .list-item .amt, .statusbar { letter-spacing: 0; }
```

- **한글은 −0.04em, 숫자는 0.** 금액에 음수 자간을 주면 자릿수가 붙어 읽히므로 원본이 예외를 따로 뺐다. 새 화면에서도 금액·날짜·시각은 이 예외 목록에 넣는다.
- **`word-break: keep-all`.** 한글이 단어 중간에서 끊기지 않게 한다. 짧은 폭에서 줄바꿈이 어색하면 `<br>`로 직접 끊는다(원본이 제목에서 쓰는 방식).

### ⚠ Plus Jakarta Sans가 번들에 없다

원본은 Google Fonts CDN에서 불러온다.

```html
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@800&display=swap">
```

하지만 저장소 [frontend/public/fonts/](frontend/public/fonts/)에는 `PretendardVariable.subset.woff2`와 `OFL.txt`뿐이다. **오프라인·CSP 정책상 CDN을 쓰지 않기로 한 저장소 방침**([public/fonts/README.md](frontend/public/fonts/README.md))과 충돌하므로, 로고를 구현하려면 800 웨이트만 서브셋해서 번들해야 한다. Plus Jakarta Sans는 OFL이라 임베딩에 문제는 없다.

---

## 3. 형태·여백·모션

| 항목 | 값 | 비고 |
|---|---|---|
| 카드 라운드 | `--radius: 20px` | 카드·모달·시트 |
| 버튼·입력 라운드 | `--radius-sm: 12px` | 칩은 16px, 태그·뱃지는 20px(알약) |
| 본문 좌우 여백 | 24px | `.pad { padding: 12px 24px 28px }` |
| 카드 내부 여백 | 24px | `.card { padding: 24px }` |
| 기본 버튼 | 20px 패딩 / 17px / 700 | `.btn` |
| 터치 반응 | `transform: scale(.98)`, 80ms | 누르는 즉시, 되돌아오지 않음 |

### 이징 — 두 가지만 쓴다

원본은 커브를 두 종류로 나눠 쓴다. **의미가 다르므로 섞지 않는다.**

| 커브 | 쓰임 | 원본 사례 |
|---|---|---|
| `cubic-bezier(.2, .8, .2, 1)` | **상태 전이** — 값이 바뀌는 모든 것 | 진행바 폭, 게이지 호, 바텀시트, 폼 등장 |
| `cubic-bezier(.2, 1.4, .4, 1)`<br>`cubic-bezier(.2, 1.2, .4, 1)` | **등장** — 없던 것이 나타날 때만 | 마이룸 소품 도착, 모달, 미니 토스트 |

즉 **오버슈트(튕김)는 디자이너의 의도이고, 등장 연출 전용이다.** 금액 차감·게이지 변화 같은 상태 전이에 튕김을 쓰면 "숫자가 지나쳤다 돌아온다"로 읽히므로 금지한다.

---

## 4. 이 브랜드에서 쓰지 말아야 할 것

원본이 **일관되게 피하고 있는** 것들이다. 새 화면에서도 유지한다.

### ① 색 있는 배경 위의 회색 글자

원본은 색 면 위에 항상 **같은 계열의 짙은 색**을 얹는다 — `--blue-weak` 위엔 `--blue-dark`, `--green-weak` 위엔 `#054D28`. 회색(`--t3`)을 색 면에 올린 곳이 한 군데도 없다. 이 규칙을 그대로 따른다.

### ② 흰 카드 안의 흰 카드

깊이가 필요한 곳에서 원본은 **안쪽을 `--bg` 평면으로** 처리한다(`.grass-card` 안의 `.kp-tile`, `.crow` 안의 `.cin`, `#s-report` 안의 `.cmp`). 흰 카드에 그림자를 얹어 또 겹치는 구조는 없다. **카드는 흰색 1겹, 그 안은 회색 평면.**

### ③ 상태 전이에 붙는 튕김

§3 참조. 오버슈트는 등장 전용이다.

### ④ 경고·비난 톤

`--red #D03238`는 원본에서 **금액 부호와 알림 점(`.ndot`, `.bell-dot`)에만** 쓰인다. 빨간 배경 블록도, 경고 삼각형도, 하락 화살표도 없다. 타깃 고객이 검증된 수준으로 거부하는 것이 "예산 초과를 반복해서 지적하는 알림"이므로([03_타깃고객](reference/기획/01_고객/03_타깃고객.md)), 이 절제를 유지한다.

### ⑤ 광고처럼 보이는 상품 노출

금융상품은 **정보성 비교만** 하고 판매·중개가 없다. 원본 `s-compare` 하단이 이 원칙을 문구로 못 박아 뒀다 — *"추천은 광고비가 아니라 최근 3개월 소비 패턴 매칭 순이에요."* 이 문구와 `가입은 각 사로` 성격의 안내는 **뺄 수 없는 요소**다(P0 정책, [시장경쟁분석 §8.1](reference/기획/02_시장/01_시장경쟁분석.md)).

---

## 5. 화면 만들 때 자동으로 나오는 기본값 5가지

브랜드와 무관하게 저절로 튀어나오는 목록이다. 원본이 이미 어떻게 처리했는지와 함께 둔다.

| 자주 나오는 기본값 | 원본의 처리 |
|---|---|
| 글꼴이 Inter·Roboto·시스템 기본값 | ✅ Pretendard + Plus Jakarta Sans로 지정 (§2) |
| 보라→파랑 그라데이션 배경 | ✅ 그라데이션은 쓰되 **전부 같은 계열 2색, 낮은 대비** — 보라·파랑 조합 없음 |
| 카드 안에 카드 | ✅ 안쪽은 `--bg` 평면 (§4 ②) |
| 색 있는 배경 위 회색 글자 | ✅ 같은 계열 짙은 색 사용 (§4 ①) |
| 튕기는 바운스 | ⚠ **등장에는 의도적으로 씀.** 상태 전이에는 금지 (§3) |

그라데이션을 전면 금지하지 않는 이유: AI 티가 나는 건 색상이 아니라 *의도 없이 두 색을 섞어 흘린다*는 점이다. 원본의 그라데이션은 전부 이유가 있다 — `.cta-fixed`는 스크롤 가림막, `.strip`은 마이룸 진입 카드, `.bn.msn`/`.bn.cardbn`은 배너 구분. **새 그라데이션을 만들 땐 같은 계열 2색으로 제한하고, 왜 필요한지 한 줄로 답할 수 있어야 한다.**

### 화면 요청용 프롬프트

```text
<만들 화면 설명>을 만들어 주세요.
DESIGN.md의 색·글꼴·라운드·이징을 그대로 쓰고, 아래는 쓰지 마세요.

- Inter, Roboto, 시스템 기본 글꼴 (본문 Pretendard, 로고만 Plus Jakarta Sans)
- 계열이 다른 두 색을 섞은 그라데이션 (같은 계열 2색만, 이유가 있을 때만)
- 흰 카드 안에 흰 카드 (안쪽은 --bg 평면)
- 색 있는 배경 위의 회색 글자 (같은 계열 짙은 색으로)
- 상태 전이에 오버슈트 이징 (튕김은 등장에만, 전이는 cubic-bezier(.2,.8,.2,1))
- 빨간 배경 블록·경고 아이콘·하락 화살표
- 광고처럼 보이는 상품 배너

금액·날짜·시각은 letter-spacing: 0으로, 나머지 한글은 -0.04em으로 맞춰 주세요.
다 만든 뒤 위 항목을 하나씩 점검하고 결과를 알려 주세요.
```

---

## 6. 코드가 원본과 다른 곳 — 전부 의도된 것

[frontend/src/styles/app.css](frontend/src/styles/app.css)는 원본 토큰을 거의 그대로 옮겼다. **다른 값은 하나뿐이다.**

| 토큰 | 원본 | 코드 | 이유 |
|---|---|---|---|
| `--t3` | `#868685` | `#6B6E6B` | 배경 위 4.5:1 미달이라 명도만 낮춤 |

그 외에는 **덧붙이기만** 했다. [tokens.css](frontend/src/styles/tokens.css)가 텍스트 전용 색을 따로 만들어 둔다 — `--blue-t`/`--green-t #0B7C39`, `--red-t #C0282E`, `--amber-t #A35F00`, 그리고 흰 글자를 받는 브랜드 면 `--blue-surface #00893E`(4.52:1).

**이 분리가 이 저장소의 핵심 규칙이다.** 색상(Hue)은 원본 그대로 두고 명도만 낮춘 값을 **글자로 칠할 때만** 쓴다. 아이콘·그래프까지 어둡게 하면 디자인 인상이 무너지므로 비텍스트는 원본 값을 유지한다. 근거는 KWCAG 2.2 / KS X 3253이고, tokens.css 상단 주석에 측정값과 함께 남아 있다.

> 새 색을 추가할 때도 같은 방식으로 한다 — 면 값 하나, 그 위의 글자 값 하나를 짝으로 만들고 대비를 측정해 주석에 적는다(`.tag-sanct` 옆의 `/* 4.64:1 */`처럼).

**초기 목업 앱(`frontend-moa`)의 파랑이 아직 남아 있다.** 그 앱은 0724 프로토 기반이라 `--blue: #3182F6`(토스 파랑)이었고, 앱 자체는 2026-08-18 폐기·삭제했지만 **그 값이 이미 새어 들어와 있다** — [app.css:876](frontend/src/styles/app.css#L876)의 선물상자 채움(`linear-gradient(#7FA9F4, #3182F6)`). 초록 브랜드 앱에서 이 한 곳만 파랑이다. **정리 대상** — 앱을 지웠다고 이 값이 사라지지는 않는다.

---

## 7. 남은 결정 — 기획 정본과 원본이 어긋난다

디자이너 원본에는 **기획 정본이 명시적으로 기각한 것들이 들어 있다.** 어느 쪽이 최신인지 제가 판단할 사안이 아니라 그대로 적어 둔다.

| 원본에 있는 것 | 기획 정본의 기록 | 근거 |
|---|---|---|
| **포인트샵**(`s-shop`, 340P, 구매) | "보상·상점 제거" | [00_서비스개요 §6](reference/기획/00_서비스개요.md) |
| **미션 + 30P 보상** | "소비형 보상 제거", 목표는 지킬 돈 하나 | 서비스개요 §6 |
| **연속 지킨 날 + 불꽃 아이콘**(`i-flame`) | "스트릭·출석 강제 폐기" | 서비스개요 §6 |
| **방어율 게이지 27%**, 챌린지 달성률 68% | "도착률·남은 거리·페이스 제거" | 서비스개요 §6 |
| **월말 축하 연출**(박수·반짝임·색종이) | "비난 금지"의 반대편 — 과잉 축하도 같은 축 | 시장경쟁분석 §8.1 |
| **도감**(11/24 수집, 면제권) | 배지·수집 요소 | 서비스개요 §6 |

원본이 최신 결정이라면 **정본 문서(`00_서비스개요` §6, `03_여정과_성장` §0)를 갱신해야 하고**, 정본이 유효하다면 원본에서 이 기능들을 걷어내야 한다. 지금은 두 문서가 서로 다른 제품을 설명하고 있다.

### 할 일

1. `프로토타입_0804.html`을 `reference/design/` 아래 커밋한다. 정본이 저장소 밖에 있으면 대조가 불가능하다.
2. `:root` 주석의 `세이지 그린 / 오프크림`이 의도인지 잔재인지 디자이너에게 확인한다(§1).
3. Plus Jakarta Sans 800을 서브셋해 번들한다(§2). CDN은 저장소 방침 위반이다.
4. 초기 목업 앱에서 토스 파랑이 새어 들어온 [app.css:876](frontend/src/styles/app.css#L876)을 정리한다. (목업 앱 자체는 2026-08-18 삭제 — 이 값만 남았다)
5. 위 표의 6개 항목에 대해 **기획과 디자인 중 어느 쪽을 갱신할지** 결정한다.
6. `--blue`처럼 이름과 값이 어긋난 변수를 `--brand` 등으로 정리한다. "blue라는 이름의 초록"이다. (앱이 하나로 정리돼 값이 갈릴 일은 없어졌다)

---

## 8. 아직 정하지 않은 것

- **다크 모드** — 원본에 없다. `--bg #F8F9FA` 기반 팔레트가 어떤 값으로 대응되는지 미정
- **카테고리 아이콘 배경 6색**(`--c-food` 등) — 원본은 개별 화면에 인라인으로 흩어져 있고 토큰화되지 않았다
- **마이룸 픽셀 아트의 색 체계** — base64 PNG로 들어 있어 토큰과 연결되지 않는다. 배경·바닥 교체 시 캐릭터 색과의 조화 규칙이 없다
