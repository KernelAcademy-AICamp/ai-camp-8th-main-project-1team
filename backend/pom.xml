<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<!-- 4.0.x 최신 패치에 고정. 4.1.0은 임베디드 H2 자동설정의 Hibernate Dialect 판정이 바뀌어
		     (h2 dev 프로파일은 datasource url 미지정 → 임베디드 자동구성 의존) 컨텍스트 로딩이 깨진다
		     ("Unable to determine Dialect without JDBC metadata"). 마이그레이션 전까지 4.0.7 유지. -->
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.7</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.finntech</groupId>
	<artifactId>backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>backend</name>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-h2console</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<!-- RFC 3161 Time-Stamp Protocol (감사로그 계층 3). ASN.1을 직접 만들지 않기 위해 씀.
		     Argon2PasswordEncoder도 이 라이브러리를 쓴다 — 그래서 admin 인증에 새로 들일 것이
		     사실상 spring-security-crypto 하나뿐이다. -->
		<dependency>
			<groupId>org.bouncycastle</groupId>
			<artifactId>bcpkix-jdk18on</artifactId>
			<version>1.85</version>
		</dependency>

		<!-- admin 비밀번호 해시(Argon2id) 전용.
		     **spring-boot-starter-security를 들이지 않는다** — 그것을 넣으면 로그인 필터·CSRF·세션이
		     통째로 딸려 와 기존 API가 갑자기 401을 내기 시작한다. 이 앱은 지금까지 인증이 없었으므로
		     그 충격이 크다. 필요한 것은 PasswordEncoder 하나뿐이라 crypto 모듈만 쓴다. -->
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-crypto</artifactId>
		</dependency>

		<!-- KMS envelope encryption (Phase 2) — 개인정보 컬럼 암호화 키를 코드·.env에 두지 않는다. -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>kms</artifactId>
			<version>2.31.78</version>
		</dependency>

		<!-- 감사 앵커 사본(Phase 5-1) — S3 Object Lock(Compliance) 버킷에 남긴다.
		     TSA 토큰이 위조 대상과 같은 DB에 있으면, DB를 갈아엎고 도장을 새로 받아
		     바꿔치기하는 공격이 통과한다. 밖에 옛 도장이 남아 있어야 드러난다. -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>s3</artifactId>
			<version>2.31.78</version>
		</dependency>

		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- 운영 MySQL 스키마 소유자(H2→MySQL 전환, Phase 6). h2 프로파일은 비활성(ddl-auto=update 유지),
		     mysql 프로파일만 flyway+validate. Spring Boot 4는 자동설정이 기술별 모듈이라
		     spring-boot-flyway가 있어야 flyway-core가 실제로 작동한다(mydata와 동일 근본원인). -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-mysql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
