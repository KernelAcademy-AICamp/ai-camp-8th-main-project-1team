-- 받은 혜택 금액 칸을 없앤다 — 실 마이데이터가 주지 않는 값이다.
--
-- ─────────────────────────────────────────────────────────────────────────────
-- ★ 이 파일은 V28 이었다. 번호만 V31 로 옮겼다 (2026-08-11) — 내용은 한 글자도 안 고쳤다.
--
-- **develop 에 V28 이 이미 있다.** 이 브랜치를 딸 때(`3def84a`)는 없었으므로 V28 로 적은
-- 것이 그 시점에는 맞았다. 그 뒤 develop 이 이렇게 움직였다:
--
--     V28__guardian_tx_source_index.sql     ← PR #161 을 타고 들어왔다 (08:51)
--     V29__merchant_category_nts_codes.sql  ← PR #161
--     V30__merchant_category_vote.sql       ← PR #161
--
-- (V28 은 원래 PR #160 의 브랜치에 있었는데, #160 이 머지된 **뒤에** 그 브랜치가 커밋을
--  두 개 더 받았고 그것이 #161 에 실려 왔다. 그래서 #160 의 스쿼시 커밋에는 V28 이 없다 —
--  이 브랜치가 못 본 이유다. **누구의 실수도 아니다.**)
--
-- 그대로 병합하면 마이그레이션 폴더에 **버전 28 이 두 개**가 된다.
--
--     V28__drop_received_benefit.sql        (이 브랜치)
--     V28__guardian_tx_source_index.sql     (develop)
--
-- Flyway 는 같은 버전 두 파일이면 기동을 거부한다:
--     FlywayException: Found more than one migration with version 28
--
-- ─────────────────────────────────────────────────────────────────────────────
-- ★ 왜 아무도 못 잡았나 — 자동 검사가 구조적으로 못 본다
--
-- 실제로 병합해 보고 재 봤다(2026-08-11):
--
--     git 병합   충돌 0건        ← 파일 **이름**이 달라 git 은 충돌로 안 본다
--     컴파일     통과
--     시험       544개 전부 통과
--     운영 기동  **실패**
--
-- 시험은 H2 인메모리에 `ddl-auto: create-drop` 이고 Flyway 가 꺼져 있다
-- (`application.yml` 기본 프로파일 `flyway.enabled: false`). Flyway 는 운영 MySQL
-- 프로파일에서만 켜진다. 즉 **시험을 다 돌려도 이 결함은 안 보이고, 실 배포 기동에서만
-- 터진다.** 그래서 번호를 미리 옮겨 둔다.
--
-- ─────────────────────────────────────────────────────────────────────────────
-- ★ 규칙 3 에 걸리지 않는다
--
-- CLAUDE.md 규칙 3(적용된 마이그레이션 불변)은 **이미 운영에 적용된** 파일을 보호한다.
-- 이 파일은 아직 운영에 올라간 적이 없으므로 번호를 옮겨도 체크섬 사고가 나지 않는다.
-- 반대로 develop 의 V28·V29·V30 은 건드리면 안 되는 쪽이라, 옮길 수 있는 것은 이쪽뿐이다.
--
-- ─────────────────────────────────────────────────────────────────────────────
-- ★★ Flyway 가 죽는 경우가 셋이다 — 셋 다 기동 시점이고, 셋 다 시험이 못 잡는다
--
-- Flyway 는 `validateOnMigrate` 가 기본 켜짐이라, 기동할 때 **파일 목록과
-- `flyway_schema_history` 표를 대조**한다. 어긋나면 마이그레이션을 안 하고 그냥 죽는다.
-- 운영은 거기에 `ddl-auto: validate` 까지 걸려 있어 스키마가 안 맞으면 두 번 막힌다.
--
--   ① 번호를 안 옮기면 — **버전 28 이 둘**
--        Found more than one migration with version 28
--      파일을 읽는 단계에서 죽으므로 DB 상태와 무관하게 **항상** 죽는다. 이것이 번호를
--      옮긴 이유다.
--
--   ② 옛 V28 을 이미 적용한 DB 를 그대로 쓰면 — **히스토리에 있는데 파일이 없다**
--        Detected applied migration not resolved locally: 28
--      번호를 옮겼으니 `V28__drop_received_benefit.sql` 은 이제 없는데, 그 DB 의
--      `flyway_schema_history` 에는 28 이 적혀 있다. Flyway 는 "누가 마이그레이션을
--      지웠다"고 보고 멈춘다. → **DB 를 새로 만들거나**(`./scripts/dev-up.sh`)
--      `flyway repair` 를 돌린다. 운영에는 적용된 적이 없으므로 개발 DB 얘기다.
--
--   ③ develop 을 안 받고 이 브랜치를 먼저 적용하면 — **나중에 낮은 번호가 온다**
--        Detected resolved migration not applied to database: 28
--      이 브랜치만 배포하면 히스토리의 최고 번호가 **31** 이 된다. 그다음에 develop 을
--      합치면 V28·V29·V30 이 들어오는데 전부 31 보다 **낮다.** Flyway 는 기본적으로
--      순서를 거슬러 적용하지 않으므로(`outOfOrder` 기본 꺼짐) 거기서 멈춘다.
--
--      → **그래서 순서가 규칙이다: develop 을 먼저 받아 병합한 뒤에 배포한다.**
--        그러면 적용 순서가 V28 → V29 → V30 → V31 로 자연스럽게 맞는다.
--        `outOfOrder=true` 로 우회하지 않는다 — 그 설정은 "낮은 번호를 나중에 적용해도
--        결과가 같다"를 가정하는데, V29 는 `merchant_category` 에 칸을 더하고 V31 은
--        `user_payment` 에서 칸을 뺀다. 지금은 서로 다른 표라 실제로 무해하지만,
--        한 번 켜 두면 **다음에 정말 순서가 중요한 마이그레이션이 왔을 때 조용히 통과한다.**
--
-- 요약하면, 이 브랜치를 배포하기 전에 할 일은 둘이다:
--   1. develop 을 병합한다 (V28·V29·V30 이 이 파일보다 앞에 놓이게)
--   2. 옛 V28 을 적용해 둔 개발 DB 는 새로 만든다
--
-- ─────────────────────────────────────────────────────────────────────────────
--
-- 마이데이터 카드 업권 API 어디에도 할인·적립액 필드가 없다. 청구 추가정보(카드-005)는
-- 이용금액·수수료·할부 정보까지고, 국내 승인내역(카드-008)도 승인금액까지다. 그런데
-- 더미 생성기는 이 값을 만들어 넣었고(그것도 난수 실적구간에서 나온 값이다), 화면은
-- 결제마다 "−1,200원"을 초록색으로 그리고 있었다.
--
-- 실데이터로 넘어가면 그 화면이 통째로 사라진다. 지금 지워야 그 위에 쌓이지 않는다.
-- 이 카드로 얼마를 아꼈는지는 카드 혜택 룰을 아는 쪽이 승인내역에서 계산할 몫이다.

ALTER TABLE `user_payment` DROP COLUMN `received_benefit`;
