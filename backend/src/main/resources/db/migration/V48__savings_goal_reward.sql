-- 목표에 '이루면 받을 소품'을 붙인다 (프로토타입_0825 s-goal3).
--
-- 왜 칼럼 하나인가: 소품 자체는 `GuardianCatalog` 가 코드로 들고 있고 표가 없다.
-- 목표는 그중 하나를 **가리키기만** 하면 되므로 코드 문자열이면 충분하다. 카탈로그에서
-- 사라진 코드가 남아도 화면이 기본 그림으로 되돌아갈 뿐 판정에는 영향이 없다.
--
-- 기존 목표는 NULL 이다 — 보상을 고르지 않고 만들어진 목표이고, 그대로 두는 것이 맞다.
ALTER TABLE savings_goal ADD COLUMN reward_code VARCHAR(40) NULL;
