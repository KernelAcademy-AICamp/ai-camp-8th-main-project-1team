#!/usr/bin/env python3
"""
[폐기됨 — 실행하지 말 것] 통장 초기잔액 사후 보정.

**이 스크립트는 더 이상 쓰지 않는다.** 통장 거래를 조회 시 계산하던 시절의 도구다.
지금은 거래가 `mydata_account_txn`에 **적재**되므로, 초기잔액만 UPDATE 하면
잔액에 비례해 계산된 **이자·세금 행이 통째로 낡는다** — 통장이 스스로 어긋난다.

같은 일을 생성이 한다: `GenerationRunner.buildLedger()`가 통장을 실제로 굴려 보고
최저 잔액이 음수면 초기잔액을 10만원 단위로 올린 뒤 **거래를 다시 만든다**(이자 포함).
여유분을 월급 배수로 주는 것도 그대로 옮겼다.

아래 원문은 그때의 근거를 남겨 둔 것이다.

원래 문서
--------
9월 말 기준으로 잔액이 마이너스가 되는 계좌의 초기잔액을 올린다.

과소비형·외식형은 생성된 카드 지출이 급여를 크게 넘는다(과소비형 평균 6,580만 vs 급여 485만/월).
그 자체는 페르소나의 사실이지만, 초기잔액이 그 적자를 감당하지 못하면 통장 잔액이 음수로 간다.
포트폴리오 시연에서 마이너스 통장이 보이는 것은 의도가 아니다.

잔액 계산은 제공자와 같은 식이어야 한다:
    잔액 = 초기잔액 + 월급누적 + 이자 − 세금 + 이체순증 − 카드결제누적
이자와 이체가 조회 시 합성되던 값이라 SQL만으로는 알 수 없어, 제공자 API를 호출해
9월 말 시점의 실제 잔액을 받아 부족분을 구했다.
"""
import sys

sys.exit(
    "[폐기됨] scripts/fix-account-balance.py 는 실행하지 않는다.\n"
    "  통장 거래가 mydata_account_txn 에 적재되므로 초기잔액만 고치면 이자·세금이 낡는다.\n"
    "  같은 보정을 GenerationRunner.buildLedger() 가 생성 시점에 수행한다."
)
